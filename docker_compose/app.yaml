services:
  fastapi:
    build: ..
    command: uvicorn --factory app.main:create_app --reload --host 0.0.0.0 --port 8000
    volumes:
      - ..:/code
    ports:
      - "8000:8000"
    networks:
      - dstuhomework_network
    container_name: dh-fastapi

networks:
  dstuhomework_network:
    driver: bridge
